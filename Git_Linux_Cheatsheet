Git Linux Cheatsheet

Linux Cheatsheet
1. pwd, displays the current working directory
2. mkdir my_directory, creates a new directory name "my_directory"
3. cd /path/to/directory, changes the current directory to the specified directory
4. ls -l, displays files and directories with detailed informatio
5. rm file.txt, deletes the file named "file.txt"
6. cp -r directory destination, copies the directory  "directory" and its contents to the specified destination
7. mv file.txt new_name.txt, renames the file "file.txt" to "new_name.txt"
8. touch file.txt, creates an empty file named "file.txt"
